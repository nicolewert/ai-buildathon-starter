{
  "name": "puppeteer-mcp-server",
  "version": "1.0.0",
  "description": "Puppeteer MCP server for browser automation in hackathon starter",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "npm run build && node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.2",
    "puppeteer": "^21.11.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}